.. entry:: Fail-closed TestPyPI rehearsal
   :type: build

   Added a no-upload frontend publication selector, strict metadata dry-run,
   and a manual TestPyPI Trusted Publishing workflow whose OIDC permission is
   isolated to an explicitly approved environment-gated job.
