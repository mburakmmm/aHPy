Add a frontend-independent handwritten HPy module to the unchanged-binary
portability artifact and execute it before generated function/type corpora, so
PyPy bridge crashes and GraalPy loader gaps can be reported with a minimal
public-HPy reproducer.
Persist a schema-versioned target report even when integrity checks, a process
exit, or a signal fails the smoke test, and upload it unconditionally from the
cross-interpreter workflow.
