.. entry:: Promote reviewed hosted performance budgets

   Promote the exact 20%-headroom proposal from five immutable Ubuntu
   CPython 3.11 / HPy 0.9 samples into enforced release budgets.  Record the
   calibration source, artifact digests, payload hashes, runtime and resource
   distributions, and keep the new candidate's same-HEAD hosted validation as
   a separate fail-closed gate.
