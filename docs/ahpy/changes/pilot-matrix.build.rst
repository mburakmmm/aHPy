Add a fail-closed, machine-readable PRD-8 pilot matrix for cython-package-example,
murmurhash, frozenlist, and the deliberately blocked NumPy C-API bezier
source.  Validate exact commits, licenses, checkout paths, expected initial
diagnostics, and recorded port changes; add deterministic checkout/scan
evidence tooling, a NumPy C-API scanner rule, a library-author porting guide,
and a compatibility issue form.

Add the first maintained port fixture for the pinned
``cython-package-example`` 0.1.7 sources.  Build its three selected modules
through HPy setuptools Universal mode, audit every generated source and
binary, and execute the upstream answer/Fibonacci/data semantics in normal,
Trace, and Debug ``LeakDetector`` modes with retained JSON evidence.

Extend that pilot evidence with fail-closed exact-wheel content validation,
dependency-free target installation, installed normal/Trace/Debug execution,
and environment-bound seven-repeat median ``axpy``/Fibonacci comparisons
against equivalent Python functions.  Keep the CPython/host wheel tag and the
absence of an enforced performance ceiling explicit.

Make blocked-pilot diagnostic quality executable: pin bezier's two NumPy
C-API findings to exact upstream source positions 37:1 and 38:1, validate the
manifest path/action relationship, and fail the matrix expectation when a
required location disappears or shifts.

Correct murmurhash's pinned physical header paths, add exact-pilot filtering
to the checkout runner, and introduce a maintained fixed-width scalar adapter
that links the exact upstream MurmurHash3 C++ implementation while keeping
all pointer and buffer ownership inside the native shim.  Validate held and
``with nogil`` calls against an independent oracle in normal/Trace/Debug and
prove conversion failures occur before native entry; retain checkout and run
JSON separately in CI without claiming the upstream bytes API.

Add a maintained frozenlist supported-subset port with an object-valued HPy
field, cyclic-GC collection, same-module inheritance, mutation/freeze/hash
semantics, constructor-failure cleanup, and normal/Trace/Debug evidence.  Keep
C++ atomic/free-threading, iterator, rich-comparison, copy/deepcopy, and
MutableSequence registration claims explicitly outside the result.

Merge complete pinned checkout/scan evidence with the three integration
reports on a per-pilot, per-gate basis.  Validate timezone-qualified ordering,
exact provenance and explicit port contracts, reject equal-time conflicts,
and retain the matrix, integration JSON, and rendered compatibility dashboard
together in CI.

Extract a checksummed 21-case Universal semantic protocol from Cython test
paths.  Add a standard-library runner with explicit surface-to-module mapping,
typed JSON values, exact exception-chain checks and separate normal/Trace/Debug
reports so other language frontends can reuse the corpus without Cython
compiler internals.

Promote recurring pilot edits into stable scanner guidance: classify
module-level compiled entry points, cross-module Cython C-API imports, direct
CPython cimports and ``libcpp`` runtime state with dedicated action IDs.  Bind
cypack and frozenlist's pinned initial-scan expectations to those precise
rules instead of accepting a generic unsupported-node result.
