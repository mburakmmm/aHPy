Add a schema-versioned non-setuptools Universal HPy build API and CLI. It
discovers the selected HPy toolchain, emits auditable argv-vector plans,
enforces Universal include/define/runtime/suffix/export rules, refuses unsafe
output reuse, audits the linked binary, and passes direct normal/Debug loading.
