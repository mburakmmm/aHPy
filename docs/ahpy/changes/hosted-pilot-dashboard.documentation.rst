Publish the four-pilot compatibility dashboard from verified hosted evidence,
retain the original JSON inputs with payload hashes, and record the exact
executed source tree and artifact identity. Reconcile the pilot result and
dashboard roadmap entries while preserving partial and blocked port scopes
and the separate release performance gates.
