Repair fork-specific mandatory CI gates after the current Cython rebase.  The
wheel changelog probe now discovers distribution metadata independently of the
project name, the handwritten HPy setup fixture defers its HPy dependency
until an actual build, and codespell recognizes the MSVC ``/Iinclude`` test
argument without suppressing ordinary spelling diagnostics.
