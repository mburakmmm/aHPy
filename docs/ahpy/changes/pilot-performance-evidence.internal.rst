Require complete non-release performance evidence for every executable PRD-8
pilot integration. Cypack, the murmurhash scalar adapter, and the frozenlist
supported subset record comparable seven-sample Python-reference workloads
with exact environment provenance. The deliberately blocked NumPy pilot emits
an explicit reason and no fabricated ratio. Dashboard ingestion fails closed
on missing, malformed, non-finite, budget-enforced, or contradictory payloads.
