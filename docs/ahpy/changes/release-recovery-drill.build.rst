Add a no-publication release recovery drill that creates an isolated release
line and provenance-bearing correctness backport, requires the original
regression test and unchanged support tier, and retains JSON evidence in the
Universal workflow. Machine-readable recovery policy requires reasoned yanks
and replacement versions for ordinary defects, limits deletion to exceptional
incidents, and keeps security fixes private until coordinated disclosure.
