.. entry:: Clean aHPy release-artifact onboarding
   :type: build

   Added a clean source-distribution safety audit, no-index wheel build, fresh
   virtual-environment install, maintained PEP 517 example build, normal/Debug
   execution, and verified uninstall/reinstall cycles for both the
   ``aHPy-compiler`` frontend and example package. HPy and setuptools build
   inputs are exact pinned wheels and the evidence record includes SHA-256
   digests.
