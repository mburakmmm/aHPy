Add a machine-readable production documentation contract covering the exact
user, contributor, architecture, debugging and release corpus. Its validator
requires critical operational headings, documentation-index reachability and
all local Markdown targets, rejects repository escapes and missing files, and
retains schema-versioned JSON evidence in the Universal workflow.
