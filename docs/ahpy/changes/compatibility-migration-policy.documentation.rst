Formalize the production compatibility and migration policy across source,
runtime semantics, generated source, artifact, CLI/configuration and diagnostic
surfaces. Preview and stable removal rules, notice channels, regression
evidence and owner-approved correctness/security exceptions are now validated
by the machine-readable maintenance contract and retained as Universal CI JSON
evidence.
