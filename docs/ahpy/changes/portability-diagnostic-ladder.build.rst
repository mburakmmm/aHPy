Add generated constant-only and single-function rungs to the unchanged
Universal HPy portability artifact, and execute them plus the generated heap
type corpus before the large function corpus so alternate-interpreter failures
identify the first unsupported generated-code boundary.
