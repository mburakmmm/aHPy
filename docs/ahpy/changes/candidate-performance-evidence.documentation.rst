.. entry:: Close hosted release-performance evidence
   :type: documentation

   Record the exact PR #12 merge-checkout run, job, artifact, archive digest,
   and benchmark payload hash that passed all 19 promoted release ceilings,
   and close PRD-7's independent same-HEAD candidate gate.  Also retain the
   first green hosted Dependency Review and Python/C++ CodeQL run for the
   versioned PRD-9 security automation.  Refresh the PyPy/GraalPy portability
   audit from the same run, reducing PyPy's first failure to generated
   Fibonacci semantics and retaining both artifact digests.
