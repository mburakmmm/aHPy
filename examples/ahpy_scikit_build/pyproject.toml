[build-system]
requires = [
    "aHPy-compiler==3.3.0.1.dev0",
    "hpy==0.9.0",
    "scikit-build-core==1.0.3",
    "cmake==4.3.4",
    "ninja==1.13.0",
]
build-backend = "scikit_build_core.build"

[project]
name = "ahpy-scikit-build-example"
version = "0.0.0"
requires-python = ">=3.11"

[tool.scikit-build]
cmake.version = ">=3.26"
wheel.packages = []
